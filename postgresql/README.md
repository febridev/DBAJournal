# PostgreSQL Administration

Administration runbooks and reference guides for PostgreSQL on Ubuntu.

## Contents
- **Installation**: Runbook for installing PostgreSQL (`runbook-installation-ubuntu.md`).
- **Replication**: Guides on streaming replication and log-based shipping standby (`streaming-replication-ubuntu.md`, `log-based-shipping-standby-ubuntu.md`).
- **Failover**: Procedure for failover in streaming replication (`failover-streaming-replication.md`).
- **Parameters**: Reference guide for PostgreSQL parameters (`parameter-refrence.md`).
