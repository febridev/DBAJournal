# Setup DNS Server
<p> This doc is for demo purpose, for example if you already have oracle RAC on your environment basically the Oracle RAC can't access using dns names.
So you need setup separately DNS Server like Production environment.
And The goal is you can have env on your local env is like production. </p>

# Environment
- Linux Ubuntu
- bind9 package
- have 2 NIC (for local env and public)

# Installation

# Verify
