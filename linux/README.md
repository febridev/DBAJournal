# Linux Administration Notes

This directory contains general Linux administration guides and notes.

## Contents
- **Storage**: Guide on LVM partition creation (`lvm_partisi_creat.md`).
- **Networking**: Reference for networking commands and domain configuration (`network_command.md`, `network-domain.md`).
- **DNS**: Demo for setting up a DNS server (`setup_dns_server_as_demo.md`).
